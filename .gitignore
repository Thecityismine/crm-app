node_modules
dist
.env.local
.env
*.local
.DS_Store

# Nested duplicate folder
crm-app/

# CSV data exports (root-level only)
/contacts/

.vercel
.env*.local

# Local Claude Code config and worktrees (worktrees hold a full app copy)
.claude/

# Telegram bot token — kept locally, never committed
crm_telegram_bot_key.txt
